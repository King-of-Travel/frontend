export function checkFieldNoError(data, errors) {
  for (const key in data) {
    let dataValue = data[key].length >= 1;
    let errorValue = errors[key].length === 0;
    
    if(dataValue && errorValue) continue;

    return false;
  }

  return true;
}
